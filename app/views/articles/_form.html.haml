= simple_form_for(@article, html: { class: 'form-horizontal' }) do |f|
  = f.input :title
  = f.input :content
  = f.collection_select :category_id, Category.all.order("name ASC"), :id, :name, { prompt: "Choose a Category" }
  %br/
  = f.submit
